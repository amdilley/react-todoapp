React = require('react')

TodoApp = React.createClass
  getInitialState: ->

  render: ->
    return (
        <div class="todo-app">
          <h1>Test</h1>
        </div>
      )

module.exports = TodoApp